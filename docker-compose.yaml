services:
  cra-farewell:
    build: 
      context: .
    ports:
      - "3030:3000"
    networks:
      app_net:
        ipv4_address: 10.10.10.2
    deploy:
      resources:
        limits:
          cpus: 1.0
          memory: "1024M"

networks:
  app_net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.10.0/24
